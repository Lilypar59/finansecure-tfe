# 🔒 Docker Security Architecture: Visual Guide

---

## PROBLEM #1: Image Tagging Issue

```
┌──────────────────────────────────────────────────────────────────┐
│ WHAT'S HAPPENING NOW (RISKY)                                     │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  docker-compose.yml                                             │
│  ├─ postgres:15-alpine   ← Implicit "latest"                   │
│  ├─ nginx:alpine         ← Implicit "latest"                   │
│  └─ pgadmin4:latest      ← Explicit "latest"                   │
│                                                                  │
│  Timeline:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Day 1                                                   │   │
│  │ $ docker pull postgres:15-alpine                       │   │
│  │ → pulls postgres:15.3-alpine ✅ Works                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Day 30                                                  │   │
│  │ $ docker pull postgres:15-alpine                       │   │
│  │ → pulls postgres:15.4-alpine 💥 BREAKS               │   │
│  │   - Missing feature developer relied on              │   │
│  │   - Changed configuration format                      │   │
│  │   - Container won't start                             │   │
│  │                                                       │   │
│  │ Problem: NO VISIBILITY into what changed!            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ SOLUTION (SAFE)                                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  docker-compose.yml                                             │
│  ├─ postgres:15.3-alpine     ← Explicit version 🔒             │
│  ├─ nginx:1.25.4-alpine      ← Explicit version 🔒             │
│  └─ pgadmin4:8.4-alpine      ← Explicit version 🔒             │
│                                                                  │
│  Timeline:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Day 1 → Day 365                                         │   │
│  │ $ docker pull postgres:15.3-alpine                     │   │
│  │ → ALWAYS pulls postgres:15.3-alpine ✅ Reproducible   │   │
│  │   Same image, same behavior                            │   │
│  │   Full control over when to update                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  When ready to upgrade:                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Plan: Test postgres:15.4 in staging first             │   │
│  │ 1. Update docker-compose.yml locally                   │   │
│  │ 2. Test in staging environment (1 week)                │   │
│  │ 3. Document any breaking changes                       │   │
│  │ 4. Update production after approval                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## PROBLEM #2: Hardcoded Secrets in Dockerfile

```
┌──────────────────────────────────────────────────────────────────┐
│ ATTACK SCENARIO: Secrets Exposed                                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  FinanSecure.Auth/Dockerfile:                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine      │   │
│  │ ...                                                    │   │
│  │ ENV JWT_SECRET_KEY="your-secret-key-..."     🔴       │   │
│  │ ENV JWT_ISSUER="FinanSecure"                 ✓         │   │
│  │ ENV JWT_AUDIENCE="FinanSecure.Client"        ✓         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Step 1: Build image                                            │
│  $ docker build -f FinanSecure.Auth/Dockerfile .               │
│  → Creates layer with JWT_SECRET_KEY                           │
│                                                                  │
│  Step 2: Attacker gets the image                               │
│  $ docker history finansecure-auth:latest                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ IMAGE          COMMAND                                 │   │
│  │ ...                                                    │   │
│  │ abc123...      ENV JWT_SECRET_KEY="your-secret..."  │   │
│  │ def456...      RUN apk add --no-cache curl          │   │
│  │ ...                                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Step 3: Attacker extracts layer                               │
│  $ docker save finansecure-auth | tar -xf -                   │
│  → Can read every layer and find secrets                      │
│                                                                  │
│  Impact:                                                        │
│  ✗ Secret visible in Docker Hub/ECR                           │
│  ✗ Secret visible in docker history                           │
│  ✗ Secret visible in docker inspect                           │
│  ✗ Developers can see it when pulling image                   │
│  ✗ CI/CD logs might show it during builds                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ SOLUTION: Secrets Only in Environment Variables                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  FinanSecure.Auth/Dockerfile:                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine      │   │
│  │ ...                                                    │   │
│  │ ENV JWT_ISSUER="FinanSecure"                 ✓         │   │
│  │ ENV JWT_AUDIENCE="FinanSecure.Client"        ✓         │   │
│  │ # JWT_SECRET_KEY is NOT here                  ✅       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  docker-compose.yml:                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ services:                                              │   │
│  │   finansecure-auth:                                    │   │
│  │     environment:                                       │   │
│  │       JwtSettings__SecretKey: ${JWT_SECRET_KEY} ✅     │   │
│  │       # This variable ONLY exists in .env             │   │
│  │       # NOT in docker-compose.yml source code         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  .env file (gitignored - never committed):                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ JWT_SECRET_KEY=aF8kL@pQ#9xM2vR5!jH7...     🔒          │   │
│  │ (exists locally, not in git, not in image)            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Now if attacker gets the image:                               │
│  $ docker history finansecure-auth:latest                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ IMAGE          COMMAND                                 │   │
│  │ ...                                                    │   │
│  │ abc123...      ENV JWT_ISSUER="FinanSecure"           │   │
│  │ def456...      RUN apk add --no-cache curl            │   │
│  │ ...                                                    │   │
│  │ # NO JWT_SECRET_KEY here! ✅                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## PROBLEM #3: localhost vs Service Name

```
┌──────────────────────────────────────────────────────────────────┐
│ localhost:8080 - Only works in SINGLE container                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Environment 1: LOCAL DEVELOPMENT                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Docker Compose (Single Host)                            │   │
│  │                                                          │   │
│  │ Container: finansecure-transactions                    │   │
│  │   $ curl http://localhost:8080/health                 │   │
│  │   ❌ ERROR: localhost = THIS CONTAINER                │   │
│  │           Not pointing to auth-service!               │   │
│  │                                                          │   │
│  │ CORRECT: finansecure-auth (service name)              │   │
│  │   $ curl http://finansecure-auth:8080/health          │   │
│  │   ✅ Works: Docker DNS resolves to auth service IP    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Environment 2: KUBERNETES                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Pod: finansecure-transactions                          │   │
│  │   $ curl http://localhost:8080/health                 │   │
│  │   ❌ ERROR: localhost = THIS POD                       │   │
│  │           Not pointing to auth-service!               │   │
│  │                                                          │   │
│  │ CORRECT: finansecure-auth (service name)              │   │
│  │   $ curl http://finansecure-auth:8080/health          │   │
│  │   ✅ Works: K8s DNS resolves to service IP            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Environment 3: AWS ECS                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Task: finansecure-transactions                         │   │
│  │   $ curl http://localhost:8080/health                 │   │
│  │   ❌ ERROR: localhost = THIS TASK                      │   │
│  │           Not pointing to auth-service!               │   │
│  │                                                          │   │
│  │ CORRECT: finansecure-auth (service discovery name)    │   │
│  │   $ curl http://finansecure-auth:8080/health          │   │
│  │   ✅ Works: ECS service discovery resolves to IP      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Environment 4: AWS EC2 (Static IPs)                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Instance: finansecure-transactions (10.0.1.100)        │   │
│  │   $ curl http://localhost:8080/health                 │   │
│  │   ❌ ERROR: localhost = THIS INSTANCE                  │   │
│  │           Not pointing to auth-service!               │   │
│  │                                                          │   │
│  │ CORRECT: 10.0.1.50 (other instance IP) OR             │   │
│  │          Internal Load Balancer DNS                    │   │
│  │   $ curl http://auth-service.internal:8080/health     │   │
│  │   ✅ Works: DNS resolves to auth service              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ SOLUTION: Use Service Names (Works Everywhere)                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Dockerfile: ENV AUTH_SERVICE_URL=http://finansecure-auth:8080 │
│                                                                  │
│  This works in:                                                 │
│  ✅ Docker Compose (Docker DNS)                               │
│  ✅ Kubernetes (K8s DNS)                                       │
│  ✅ AWS ECS (Service Discovery)                                │
│  ✅ Docker Swarm (DNS)                                         │
│  ✅ Any container orchestration platform                       │
│                                                                  │
│  Magic: Each platform's DNS resolves service names              │
│         ├─ Docker Compose: 127.0.17.1 (Docker DNS)           │
│         ├─ Kubernetes: 10.96.0.10 (CoreDNS)                   │
│         └─ AWS ECS: 169.254.169.253 (Service Discovery)       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## ARCHITECTURE: Data Flow with Security

```
┌────────────────────────────────────────────────────────────────────────────┐
│ PRODUCTION DEPLOYMENT (AWS EC2)                                            │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Internet                                                                   │
│     │                                                                       │
│     └──→ [AWS Load Balancer]                                              │
│            │                                                               │
│            ├──→ EC2 Instance (10.0.1.100)                                 │
│            │    ┌─────────────────────────────────────────────────────┐  │
│            │    │ Docker Network: backend                             │  │
│            │    │                                                      │  │
│            │    │  ┌────────────────────────────────────────────────┐ │  │
│            │    │  │ Container: finansecure-frontend (NGINX)       │ │  │
│            │    │  │ Port: 80                                       │ │  │
│            │    │  │ User: appuser (non-root)                       │ │  │
│            │    │  │ Cap: NET_BIND_SERVICE only                     │ │  │
│            │    │  │ RO:  read_only: true                           │ │  │
│            │    │  │                                                 │ │  │
│            │    │  │ Routes: /api/* → http://finansecure-auth:8080 │ │  │
│            │    │  └────────────────────────────────────────────────┘ │  │
│            │    │                                                      │  │
│            │    │  ┌────────────────────────────────────────────────┐ │  │
│            │    │  │ Container: finansecure-auth (ASP.NET Core)    │ │  │
│            │    │  │ Port: 8080                                     │ │  │
│            │    │  │ User: appuser (non-root)                       │ │  │
│            │    │  │ Cap: NET_BIND_SERVICE only                     │ │  │
│            │    │  │ Secrets: From AWS Secrets Manager              │ │  │
│            │    │  │  ├─ JWT_SECRET_KEY (rotated monthly)           │ │  │
│            │    │  │  └─ DB_PASSWORD (rotated quarterly)            │ │  │
│            │    │  └────────────────────────────────────────────────┘ │  │
│            │    │                                                      │  │
│            │    │  ┌────────────────────────────────────────────────┐ │  │
│            │    │  │ Docker Network: auth-network (isolated)        │ │  │
│            │    │  │ ✓ Only auth service + postgres                │ │  │
│            │    │  │ ✗ NGINX cannot access directly                │ │  │
│            │    │  │                                                 │ │  │
│            │    │  │ ┌──────────────────────────────────────────┐   │ │  │
│            │    │  │ │ Container: postgres (Database)          │   │ │  │
│            │    │  │ │ Port: 5432 (internal only)              │   │ │  │
│            │    │  │ │ User: postgres (non-root)               │   │ │  │
│            │    │  │ │ Password: From AWS Secrets Manager      │   │ │  │
│            │    │  │ │ RO: /var/lib/postgresql writable       │   │ │  │
│            │    │  │ └──────────────────────────────────────────┘   │ │  │
│            │    │  │                                                 │ │  │
│            │    │  └────────────────────────────────────────────────┘ │  │
│            │    └─────────────────────────────────────────────────────┘  │
│            │                                                               │
│            └──→ EC2 Instance (10.0.1.101)  [Reserved for scale-out]      │
│
│  Security Zones:
│  ┌─────────────────────────────────────────────────────────────────┐
│  │ PUBLIC: NGINX (port 80)                                        │
│  │         ├─ Accessible from internet ✓                         │
│  │         ├─ rate-limited ✓                                     │
│  │         └─ read-only filesystem ✓                             │
│  │                                                                │
│  │ PRIVATE: Auth Service (port 8080)                             │
│  │         ├─ Only accessible via NGINX ✓                       │
│  │         ├─ Secrets from Secrets Manager ✓                    │
│  │         └─ Restricted capabilities ✓                         │
│  │                                                                │
│  │ ISOLATED: Database (port 5432)                                │
│  │          ├─ Only accessible from auth service ✓              │
│  │          ├─ Not exposed to network ✓                         │
│  │          └─ Encrypted at rest & in transit ✓                 │
│  └─────────────────────────────────────────────────────────────────┘
│
└────────────────────────────────────────────────────────────────────────────┘
```

---

## Security Options Breakdown

```
┌─────────────────────────────────────────────────────────────────────┐
│ SECURITY OPTIONS: What They Do                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ security_opt:                                                       │
│   - no-new-privileges: true                                         │
│                                                                     │
│     Prevents: SUID/SGID privilege escalation                        │
│     Example without protection:                                     │
│       $ chmod u+s /bin/bash  # SUID bit                            │
│       $ /bin/bash  # Now running as root!                          │
│                                                                     │
│     With no-new-privileges:                                         │
│       $ chmod u+s /bin/bash                                        │
│       $ /bin/bash  # Still running as appuser                      │
│       Cannot escalate privileges ✅                                │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ cap_drop: ALL                                                       │
│                                                                     │
│     Dangerous capabilities removed:                                 │
│     ✗ NET_RAW: Can't create raw sockets (used for spoofing)        │
│     ✗ SYS_ADMIN: Can't do many admin operations                    │
│     ✗ DAC_OVERRIDE: Can't override file permissions                │
│     ✗ SYS_PTRACE: Can't trace other processes                      │
│                                                                     │
│     If container compromised, attacker can't:                      │
│     ✗ Modify other container permissions                           │
│     ✗ Trace host processes                                         │
│     ✗ Create network packets                                       │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ cap_add: NET_BIND_SERVICE                                           │
│                                                                     │
│     Allows: Binding to port 80 and 8080 (< 1024 reserved ports)    │
│     Without this: Would need to run as root for port 80            │
│                                                                     │
│     Example NGINX:                                                 │
│     ┌──────────────────────────────────────────────────────────┐   │
│     │ cap_drop: ALL                                            │   │
│     │ cap_add: NET_BIND_SERVICE                                │   │
│     │                                                           │   │
│     │ Now NGINX can:                                           │   │
│     │   $ nginx -g 'daemon off;'  # Bind to port 80 as appuser │   │
│     │                                                           │   │
│     │ But NGINX cannot:                                        │   │
│     │   $ ptrace /proc/[pid]  # Trace processes               │   │
│     │   $ iptables -A ...     # Modify firewall                │   │
│     │   $ ip link add vlan0   # Create network interfaces      │   │
│     └──────────────────────────────────────────────────────────┘   │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│ read_only: true                                                     │
│                                                                     │
│     Effect: Filesystem is read-only, no writes allowed              │
│     Requires: tmpfs for writable directories                        │
│                                                                     │
│     For NGINX:                                                      │
│       ✓ Can read: /etc/nginx/nginx.conf                            │
│       ✓ Can read: /usr/share/nginx/html                            │
│       ✗ Cannot write: /etc                                         │
│       ✗ Cannot modify: System files                                │
│                                                                     │
│     tmpfs (in-memory filesystem):                                   │
│       ✓ /var/cache/nginx      [writable via tmpfs]                │
│       ✓ /var/log/nginx        [writable via tmpfs]                │
│       ✓ /var/run              [writable via tmpfs]                │
│       ✓ /tmp                  [writable via tmpfs]                │
│                                                                     │
│     If container compromised:                                      │
│     ✓ Attacker cannot modify /etc/nginx/nginx.conf                │
│     ✓ Attacker cannot install malware to /usr/bin                 │
│     ✓ Only can write to tmpfs (lost on restart)                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Implementation Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ WEEK 1: CRITICAL FIXES (52 minutes)                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Monday (15 min)                                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 1. Update docker-compose.yml                              │ │
│  │    └─ Pin image versions (postgres, nginx, pgadmin)      │ │
│  │       Before: postgres:15-alpine                          │ │
│  │       After:  postgres:15.3-alpine                        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Monday (10 min)                                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 2. Edit FinanSecure.Auth/Dockerfile                        │ │
│  │    └─ Remove JWT_SECRET_KEY from ENV                      │ │
│  │       ❌ DELETE: ENV JWT_SECRET_KEY="..."                │ │
│  │       ✅ KEEP:   ENV JWT_ISSUER="FinanSecure"            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Monday (7 min)                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 3. Edit FinanSecure.Transactions/Dockerfile               │ │
│  │    └─ Remove DB_PASSWORD, JWT_SECRET_KEY                  │ │
│  │    └─ Change localhost:8080 → finansecure-auth:8080      │ │
│  │       ❌ DELETE: ENV DB_PASSWORD="postgres"              │ │
│  │       ❌ DELETE: ENV JWT_SECRET_KEY="..."                │ │
│  │       ✅ CHANGE: localhost:8080 → finansecure-auth:8080  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Tuesday (5 min)                                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 4. Create .dockerignore                                    │ │
│  │    └─ Copy template, customize if needed                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Tuesday (15 min)                                               │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 5. Update docker-compose.yml - Security Options            │ │
│  │    └─ Add security_opt, cap_drop, cap_add                │ │
│  │       Add to: finansecure-auth, finansecure-frontend     │ │
│  │       security_opt:                                        │ │
│  │         - no-new-privileges: true                         │ │
│  │       cap_drop:                                            │ │
│  │         - ALL                                              │ │
│  │       cap_add:                                             │ │
│  │         - NET_BIND_SERVICE                                │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Wednesday (10 min)                                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 6. Test & Verify                                            │
│  │    $ docker compose down -v                                │ │
│  │    $ docker compose up -d                                  │ │
│  │    $ docker compose ps  # All healthy?                    │ │
│  │    $ docker history finansecure-auth | grep -i secret    │ │
│  │    # Should return: (nothing) ✅                          │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Total: 52 minutes → SECURITY SCORE: 6.3/10 → 9.0/10 ✅         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

**Everything documented! Ready for implementation.** 🚀
